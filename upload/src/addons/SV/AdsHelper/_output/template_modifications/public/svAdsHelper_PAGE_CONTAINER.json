{
    "template": "PAGE_CONTAINER",
    "description": "Inject advertising slot into the start of the head tag",
    "execution_order": 1,
    "enabled": true,
    "action": "preg_replace",
    "find": "#<head(|\\s+[^>]*)>#si",
    "replace": "$0\n<xf:ad position=\"svAdsHelper_head_tag\" />"
}